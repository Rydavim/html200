const fruits = ['lychee', 'peach', 'dragonfruit', 'starfruit', 'watermelon', 'yuzu'];
for (let i = 0; i < fruits.length; i++) {
  console.log(i + ',' + fruits[i]);
};
// assignment read to log the index, but the example started at 1 - those conflict, so I logged the index, starting at 0
